import createBody from './body';
import createFooter from './footer';
import createHeader from './header';
import home from './home';

function init() {
    createFooter();
    const container = createBody();
    createHeader(container);
    home(container);
}

export default init;